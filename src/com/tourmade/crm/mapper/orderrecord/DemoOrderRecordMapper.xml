<?xml version="1.0" encoding="utf-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tourmade.crm.mapper.orderrecord.DemoOrderRecordMapper">

	<select id="queryOrderRecord" parameterType="Map" resultType="DemoOrderRecord">
		SELECT *
		FROM (
		select
		u.order_record_id as recordid,
		u.order_id as orderid,
		u.sender as sender,
		u.sender_id as senderid,
		u.sender_name as sendername,
		u.content as content,
		u.file_url as fileurl,
		u.req_time as reqtime,
		u.creat_time as creattime,
		u.isdel as isdel
		from tm_order_record u  
		where u.isdel = 0
		and u.order_id = #{id}
		order by u.creat_time
		) table_alias
		limit #{b},#{e}
	</select>

	<select id="getOrderRecord" parameterType="Map" resultType="DemoOrderRecord">
		SELECT *
		FROM (
		select
		u.order_record_id as recordid,
		u.order_id as orderid,
		u.sender_id as senderid,
		u.sender_name as sendername,
		u.content as content,
		u.file_url as fileurl,
		u.req_time as reqtime,
		u.creat_time as creattime,
		v.case_id,
		v.order_id,
		u.isdel as isdel
		from tm_order_record u  
		left join tm_order v on v.case_id = #{id}
		where u.isdel = 0
		and u.order_id = v.order_id
		order by u.creat_time
		) table_alias
		limit #{b},#{e}
	</select>
	
	<select id="countOrderRecord" parameterType="DemoOrderRecord" resultType="long">
		select count(u.order_record_id) from tm_order_record u
		where
		u.isdel = 0
	</select>
	
	<insert id="saveOrderRecord" parameterType="DemoOrderRecord" flushCache="true" useGeneratedKeys="true" keyProperty="orderrecord_id" keyColumn="GENERATED_KEY">
		insert into tm_orderrecord(
		orderrecord_name,
		country,
		language,
		creat_time,
		isdel
		)values(
		#{name,jdbcType=VARCHAR},
		#{country,jdbcType=VARCHAR},
		#{language,jdbcType=VARCHAR},
		NOW(),
		0
		)
	</insert>

	<update id="updateOrderRecord" parameterType="DemoOrderRecord" flushCache="true">
		update tm_orderrecord
		set
		orderrecord_name=#{name},
		country=#{country},
		language=#{language},
		update_time=NOW()
		where
		orderrecord_id=#{orderrecord_id}
	</update>
	
	<select id="getOrderRecordById" parameterType="int" resultType="DemoOrderRecord">
		select
		u.orderrecord_id as orderrecord_id,
		u.orderrecord_name as name,
		u.country as country,
		u.language as language,
		u.isdel as isdel
		from tm_orderrecord u where u.orderrecord_id = #{orderrecord_id}
	</select>
	
	<select id="getParameterInfo" parameterType="String" resultType="DemoList">
		select
		u.para_domain,
		u.para_value as id,
		u.chinese as text,
		u.isdisplay,
		u.isdel 
		from tm_parameter u where u.isdisplay = 1 and u.isdel = 0 and u.para_domain = #{domain}
	</select>
	
	<update id="deleteOrderRecordById" parameterType="int" flushCache="true"
		statementType="PREPARED">
		update tm_orderrecord
		set
		isdel=1
		where
		orderrecord_id = #{order_record_id}
	</update>
</mapper>